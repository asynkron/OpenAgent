{
  "name": "chat-agent-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chat agent websocket backend",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "start": "npm run build && node ./dist/index.js",
    "dev": "NODE_ENV=development npm run start"
  },
  "dependencies": {
    "@asynkron/openagent-core": "file:../../core",
    "ws": "^8.15.1"
  },
  "devDependencies": {
    "@types/ws": "^8.5.12"
  }
}
